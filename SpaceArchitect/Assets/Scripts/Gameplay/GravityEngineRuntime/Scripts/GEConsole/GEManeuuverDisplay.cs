using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Dummy component. It's editor script displays a list of the pending manuevers when GE is running. 
/// 
/// This can be useful when debugging scenes. 
/// </summary>
public class GEManeuuverDisplay : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
